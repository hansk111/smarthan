# smarthan app 👋

## 설치

```
npx create-expo-app@latest
```

첫번째 프리뷰 빌드

npm install -g eas-cli
eas build:configure
eas build --profile preview --platform android
eas build --profile development --platform android
eas update --environment preview : 동작 잘함함

login 기능 구현 : git 1. commit
mp3player 기능 구현 : git 2. commit -> error 발생함.
기본적인 기능 모두 동작 : git 3. commit
